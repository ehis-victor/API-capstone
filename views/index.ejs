<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Programming Quotes App</title>
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Rowdies:wght@300;400;700&family=Sofia&display=swap" rel="stylesheet">
        <link rel="stylesheet" type="text/css" href="/styles/main.css">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    </head>
        <body>
            <div class="whole-website">
                <%- include("partials/header.ejs") %>


                    <div class="container">
                        <div class="h1">
                            <h1>Random Programming Quotes:</h1>
                        </div>

                        <% if (locals.quote) { %>
                        <div class="card-container">
                                <div class="quote">
                                    <h2>
                                        <%= quote %>
                                    </h2>
                                </div>
                        </div>
                        
                        <div class="small">
                            <p class="texting">
                            <small class="user">Another random quote, by <strong><%= programmer %> &#128406 </strong></small>
                            </p>
                        </div>

                        <div class="input">
                            <form id="programmerForm" method="post">
                            <input id="programmer" class="buttons" type="submit" name="programmer" value="<%= programmer %>" formaction="/programmer">
                            </form>
                        </div>

                        <div class="input">
                            <form id="randomForm" method="get">
                            <input id="random" class="buttons" type="submit" name="random" value="Random" formaction="/">
                            </form>
                        </div>

                        <div class="error">
                            <% } else if (locals.programmer) { %>

                                <div class="noQuote">
                                    <p> No Quote From This Programmer</p>
                                </div>

                            <% } else { %>
                            <p class="error">
                            <%= error %>
                            </p>
                            <% } %>
                        </div>
                    </div>

                <%- include("partials/footer.ejs") %>
            </div>
        </body>
</html>